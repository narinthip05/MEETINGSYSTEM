

export interface UsersData {
    Userid:   number;           // รหัสผู้ใช้งาน (เปลี่ยนเป็น number)
    Username: string;           // ชื่อผู้ใช้งาน
    Password: string;           // รหัสผ่าน
    Role:     number;           // ตำแหน่ง (Admin, Users)
    
}
